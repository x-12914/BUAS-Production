[pytest]
minversion = 7.0
addopts = -ra
testpaths = tests
markers =
    quick: Fast manual smoke checks for VPS pre-deploy validation.
    full: Deeper integration checks for regular validation runs.
    heavy: Longer-running reliability and release checks.
